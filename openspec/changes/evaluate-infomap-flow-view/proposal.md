## Why

Directed flow can reveal patterns intentionally absent from an initially symmetrized structural topology, but different partitions are not proof of better agent answers.

## What Changes

- Create an isolated, pinned Infomap experiment and compare it against stable topology/evaluation fixtures.
- Preregister utility, stability, runtime, memory, install, and GPL/commercial licensing gates.
- Record an explicit adopt-later or stop decision without changing runtime behavior.

## Capabilities

No stable capability delta is proposed. This research-only change declares `skip_specs: true`.

## Impact

This work evaluates a potential future backend/capability. It does not change Beholder’s product contract unless a later reviewed proposal adopts it.

It may add offline experiment tooling and reports only after B and D4 fixtures are stable; it must not add a daemon, MCP, or production dependency.
