# SHIP NOW, empirically gated — Deterministic topology projection and Leiden CPM

Parent: algorithm-neutral derived topology, deterministic projection, evaluated flat Leiden CPM, independent resolutions, separate persistence/readiness and non-blocking jobs. New `topology-analysis`; modify `indexing-pipeline` only for derived-job/readiness. Non-goals: canonical mutation, semantic labels, hierarchy, query-time computation, Infomap/SBM.

## B1 — Define topology types and canonical serialization
Motivation: algorithms must not leak graph/storage types. Scope: manifests, nodes/edges/layers, partition/run metrics, keys/statuses/canonical JSON. Non-goals: projection logic/dependency. Current: `EntityRef`/`SemanticEdge`/`QueryMetadata`, existing SHA-256. Architecture: `topology-analysis` pure types; dense IDs adapter-local. API: `TopologyProjection`, `CommunityPartition`, `DerivedStatus`. Tests: golden serialization, permutation, hash sensitivity, malformed/non-finite weights. Acceptance: byte-stable sorted serialization/no third-party public types. Dependencies: none. Shipping: crate/types PR. Compatibility: additive/unused until B2. PR: `feat(topology): define derived topology contracts`.

## B2 — Build deterministic projection over all real edge kinds
Motivation: output honesty depends on filters/layers/weights. Scope: participant matrix, every relation mapping, direction manifest, test/generated/external/container treatment, repo metadata, hub presets, diagnostics. Non-goals: aesthetic weighting/community execution. Current: `WorkspaceTopology`, typed generated/evidence; repository metadata. Architecture: pure sorted transformation with no storage/query calls. API: fully serialized/versioned `ProjectionConfigV1`. Tests: every kind/relation, parallel/self/direction/hubs/generated/tests/external/unknown/row order. Acceptance: each input included/support-only/omitted with deterministic reason/count; no invented fact. Dependencies: B1. Shipping: projection/fixtures PR. Compatibility: derived-only. PR: `feat(topology): project semantic graphs deterministically`.

## B3 — Select and validate Leiden CPM backend
Motivation: native local-first preferred, candidates need audit. Scope: candidate audit, minimum features, adapter, seed/single thread, CPM, metrics, fixtures/permitted evaluation, differential oracle. Non-goals: handwritten algorithm, Python runtime dependency, production multiplex before evidence. Current: no backend; Rayon determinism unproven. Architecture: private trait only if two real implementations; otherwise one adapter. API: identity records crate/source/license/version/runtime/seed/config. Tests: empty/disconnected/singleton/self-loop/weighted/clique/LFR, repeats, oracle, targets, memory/time. Acceptance: reviewed license/source, sensible fixtures, documented repeatability, no GPL production dependency, utility gate or do-not-adopt. Dependencies: B2/A1 discipline. Shipping: gated dependency/adapter/evaluation or report. Compatibility: no default output. PR: `feat(topology): add validated Leiden CPM backend` or rejection docs.

Gate normalization: after backend eligibility, evaluate deterministic topology through an offline injection adapter in the frozen A1 harness. It changes no product interface/runtime and uses B3 deterministic output, not C2 semantic packs. It must pass preregistered correctness, non-inferiority, misleading-interpretation, and utility thresholds before C or D. D4 separately validates the integrated product path; its failure prevents default enablement even if this offline gate passed.

## B4 — Expose multiple resolutions without false hierarchy
Motivation: gamma arbitrary; levels are not architecture. Scope: coarse/standard/fine registration, independent keys/partitions, metrics, no-parent contract. Non-goals: dendrogram/best resolution/semantic naming. Current: no community contract; candidate APIs only. Architecture: serialized resolution registry; independent runs. API: `ResolutionPartition`. Tests: non-nested overlap/reordered membership/stable IDs/absent resolution. Acceptance: no mistaken parentage; traces to B3 key. Dependencies: B3. Shipping: DTO/evaluation PR. Compatibility: additive. PR: `feat(topology): publish independent resolution profiles`.

## B5a — Persist derived artifacts and readiness separately
Motivation: Mnestic GC/lifecycle cannot own rebuildable communities. Scope: SQLite/WAL/read path, immutable blobs/manifests, selected pointer/status, quota-age retention, corruption miss. Non-goals: scheduling/canonical pinning/history guarantee. Current: platform dirs/SQLx/SQLite/#88 rules; not queue DB. Architecture: `derived-state` behind Beholder port. API: exact-key put/get/select/status/gc. Tests: atomic select/crash-corrupt/concurrent reader/GC/wrong revision. Acceptance: no partial visible; canonical untouched; bounded readiness. Dependencies: B1/B2; parallel B3. Shipping: storage PR. Compatibility: removable additive DB. PR: `feat(derived-state): persist topology artifacts safely`.

## B5b — Add durable non-blocking topology jobs
Motivation: work outlives request and must recover/coalesce. Scope: job kind/payload/five attempts/coalescing/recovery/cancel/currentness/metrics. Non-goals: generic DAG/provider jobs. Current: `jobs.rs` lifecycle, not enrichment payload/publication. Architecture: immutable snapshot/materialization/compute/B5a/exact selection. API: typed submit/get/list statuses. Tests: recovery, supersession, duplicate, terminal/shutdown, publication/query concurrency. Acceptance: never blocks canonical; stale cannot select newer; failure inspectable. Dependencies: B3+B5a. Shipping: jobs/protocol PR. Compatibility: additive fields + old-client ignores. PR: `feat(daemon): run durable topology analysis jobs`.
