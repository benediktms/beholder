# RESEARCH, gated after useful B — Optional semantic architecture overlay

Parent: after B utility gate, stable units, bounded evidence packs, optional isolated provider, validated cached refinements, uncertain continuity. New `architecture-overlay`. Non-goals: canonical mutation, required LLM/query-time calls, free-form ontology, hidden merge/split, perfect recovery.

## C1 — Define minimal architecture overlay independent of communities
Motivation: partitions are not stable services/subsystems. Scope: unit IDs, boundary sources, membership evidence, parents/labels/shared-cross-cutting-orphan flags. Non-goals: provider, merge/split, ontology expansion. Current: Service/repo/contract facts/evidence; no unit type. Architecture: skeleton can exist without Leiden; communities evidence only. API: `ArchitectureOverlayV1`, `ArchitectureUnit`, boundary/uncertainty. Tests: reordered IDs/missing/multi-unit community. Acceptance: no opaque-text-only unit; every membership origin/evidence. Dependencies: B3 usefulness gate. Shipping: domain/DTO PR. Compatibility: additive derived. PR: `feat(architecture): define evidence-backed overlay`.

## C2 — Build bounded reproducible community evidence packs
Motivation: whole graph prompts cost/expose too much. Scope: deterministic member-edge-boundary sampling, snippets, omissions, context hash. Non-goals: model call/summarization. Current: B projections/partitions and `EvidenceRef`. Architecture: pure builder fixed scoring/ties/config. API: `EvidencePackV1`. Tests: row order, bounds, multibyte, selected evidence only. Acceptance: pinned inputs identical bytes/hash; omissions counted. Dependencies: C1/B4. Shipping: pack-builder PR. Compatibility: internal derived. PR: `feat(architecture): build bounded evidence packs`.

## C3 — Add schema-validated optional provider worker
Motivation: interpretation cannot enter daemon trust/latency. Scope: config, isolated protocol, limits/cancel, JSON schema, identity/cache/failures. Non-goals: hosted requirement, enrichment reuse, auto install. Current: reuse worker/cancel/trace patterns only; analyzer payload unsuitable. Architecture: separate protocol/job to derived state. API: pack key/content request and allowed overlay response. Tests: no-provider/timeout/crash/cancel/malformed/cache/hash changes. Acceptance: no query-time call; invalid never selected; complete provenance. Dependencies: C2. Shipping: protocol/worker/job PR. Compatibility: opt-in disabled. PR: `feat(architecture): run optional semantic provider worker`.

## C4 — Apply conservative traceable overlay refinements
Motivation: useful output must be bounded/reversible. Scope: label/description/parent/reparent/shared-cross-cutting-orphan; merge/split proposals only. Non-goals: canonical edits/entity creation/silent merge/split. Current: no actions. Architecture: whitelist validator + deterministic reducer. API: accepted/rejected suggestions/evidence/uncertainty/reason. Tests: conflicts/cycle/unknown/oversize/determinism/rollback. Acceptance: visible refinement traces to suggestion/evidence; conflicts reported; merge/split proposals. Dependencies: C3. Shipping: reducer/API PR. Compatibility: overlay-only. PR: `feat(architecture): apply conservative overlay refinements`.

## C5 — Track concept continuity with explicit uncertainty
Motivation: changes across revisions without false identity. Scope: same-resolution reciprocal overlap, split/merge/rename/new/disappearance/uncertain. Non-goals: perfect lineage/cross-resolution hierarchy/canonical identity. Current: no historical derived; old canonical can GC. Architecture: retained adjacent artifacts, deterministic ties. API: `ArchitectureContinuityV1`. Tests: rename/split/merge/disappear/equal/missing. Acceptance: retained exact keys required; ambiguity explicit; rebuildable while retained. Dependencies: C4 + retained B/C. Shipping: continuity PR. Compatibility: additive/no time travel. PR: `feat(architecture): report uncertain concept continuity`.
