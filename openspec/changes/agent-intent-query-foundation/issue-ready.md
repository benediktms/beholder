# SHIP NOW — Agent intent query foundation and MCP v2 migration

Parent: bounded reusable intent services and five additive MCP v2 tools; preserve current tools/legacy semantics and rationalize CLI without blind removal. Modified `query-api`; new `agent-query-api`. Non-goals: topology/community/LLM, command removal, UI migration, canonical graph changes. Design: `crates/architecture-query` above daemon/store, one immutable snapshot, strict resolution, shared budgets, deterministic compact/full-evidence output; MCP/CLI adapters and retained aliases.

## A1 — Establish repeatable agent benchmark baseline
Motivation: no repo-only/MCP correctness-cost evidence; local route counts lack attribution. Scope: frozen task corpus, weighted gold evidence, critical rubric, minimal manual-capable harness, transcripts, paired repo-only/current-MCP arms, model/prompt/revision/cache controls, repeats, availability. Non-goals: topology/semantic proof, private Fresha, hard wall-clock CI. Current: MCP smoke fixtures, benchmark commands, #91 discipline, query metadata. Architecture: deterministic manifest plus arm adapters; separate scorer; append-only raw results; paired identical controls. API/domain: benchmark-only versioned JSON. Tests: fixed fake transcripts, missing arm, token/time parser, blind rubric fixtures, cold/warm schedule, stale/truncated critical failure. Acceptance: first executable delivery; preregistration/hard floors before runs; paired baseline before product work; evidence-linked scores and hard misleading failures. Dependencies: none; gates A3-A5/B product code; A2/read-only B research parallel. Independent shipping: docs/scripts/fixtures PR. Compatibility: no runtime change. Focused PR: `test(benchmark): establish agent query baseline`.

## A2 — Specify MCP v2 intent contracts in OpenSpec
Motivation: names conceal trace/impact/context differences. Scope: schemas, bounds, errors, resolution, revision, readiness, provenance, compatibility, benchmark linkage. Non-goals: implementation, topology, CLI removal. Current: `query-api`, product-direction, archived optimization/evidence contexts. Architecture: application services above existing graph primitives; adapters below. API/domain: eight delta requirements and concrete tool tables. Tests: strict validation and scenario review against protobuf/MCP tests. Acceptance: no ambiguous default/semantic alias; every field/error has a scenario. Dependencies: may draft beside A1; dispatch consumes frozen A1 manifest. Independent shipping: planning-only PR. Compatibility: additive. Focused PR: `docs(openspec): specify agent intent query surface`.

## A3 — Add deterministic reusable query services
Motivation: avoid MCP fan-out and UI duplicate graph logic. Scope: overview/find/inspect/multipath trace/grouped impact/common envelope-resolver-bounds. Non-goals: MCP registration, CLI reorganization, communities, LLM. Current: DTO metadata/edges, immutable Mnestic snapshots, daemon query helper/indexed traversal. Architecture: `architecture-query` owns acquisition/compression; bounded fixed-shape adapter/store additions. API/domain: Beholder request/results/schema constants; no transport/Mnestic leakage. Tests: row order, revision race, ambiguity, every bound/error, evidence, old-daemon shields. Acceptance: byte-identical canonical JSON for same snapshot/runtime; no MCP dependency/blocking publication. Dependencies: A2. Independent shipping: domain/DTO/store/service PR hidden until A4. Compatibility: existing queries untouched. Focused PR: `feat(query): add bounded agent intent services`.

## A4 — Migrate MCP additively and benchmark current/new
Motivation: intent-sized tools without losing evidence/old clients. Scope: five registrations, strict A3 mapping, errors/help/smoke, arms 1-3. Non-goals: remove old tools, topology/semantic claims, unmeasured progress notifications. Current: exact-three router assertion and isolated MCP harness. Architecture: thin adapter, no fan-out/renderer duplication. API/domain: `beholder.intent.*.v1`, retain `entity_search.v2`/`traverse_graph.v2`. Tests: schemas/defaults/maxima, tool list, raw/structured equality, diagnostics, revision race, errors, benchmark. Acceptance: five bounded deterministic tools; current unchanged; measured-only report. Dependencies: A1-A3. Independent shipping: MCP/protocol/docs PR. Compatibility: additive/client shields. Focused PR: `feat(mcp): expose agent intent query tools`.

## A5 — Rationalize CLI semantic/debug namespaces
Motivation: top-level semantic/debug collisions obscure primary interface. Scope: `query`, `debug inspect`, docs/scripts migration, aliases, architecture-map slot. Non-goals: semantic changes/deleting admin-debug-benchmark-legacy commands. Current: semantic routes are daemon-client/presentation; legacy normative. Architecture: one routing table; aliases call identical handler. API/domain: CLI names only. Tests: clap snapshots/help and alias equivalence/error parity. Acceptance: coherent primary help and documented-window old invocation; no route-only usage inference. Dependencies: A2; after/beside A4. Independent shipping: CLI/docs PR. Compatibility: aliases one release or longer absent evidence/cost. Focused PR: `refactor(cli): group semantic and debug queries`.
