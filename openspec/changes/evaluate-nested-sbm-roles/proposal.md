## Why

Role-equivalence, core-periphery, bipartite, and layered structure may answer questions that assortative communities miss, but a lower description length is not evidence of agent utility.

## What Changes

- Create an isolated, pinned nested degree-corrected SBM experiment.
- Preregister model, stability, cost, licensing, and agent-task comparison gates.
- Publish an explicit stop or later-adoption decision without changing product behavior.

## Capabilities

No stable capability delta is proposed. This research-only change declares `skip_specs: true`.

## Impact

This work evaluates a potential future backend/capability. It does not change Beholder’s product contract unless a later reviewed proposal adopts it.

It is deferred until mature overlay evaluation and E completion/skipping, and must not add graph-tool, a daemon integration, or a runtime hierarchy.
