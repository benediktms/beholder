language: 'typescript'

tasks:
  build:
    command: 'pnpm build'
  check:
    command: 'pnpm check'
  release:
    command: 'pnpm tauri build --no-bundle'
    options:
      cache: false
      runInCI: false
  test:
    command: 'pnpm test'
