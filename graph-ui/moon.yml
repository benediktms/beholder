language: 'typescript'

tasks:
  build:
    command: 'pnpm build'
  check:
    command: 'pnpm check'
  release:
    command: 'pnpm tauri build --no-bundle'
    deps:
      - 'build'
    options:
      cache: false
  test:
    command: 'pnpm test'
